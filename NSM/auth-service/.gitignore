/bin/
/dist/
*.exe
*.test
coverage.out
coverage.html

.env
.env.*
!.env.example
configs/*.local.yaml

# Disposable Ed25519 development key generated by cmd/devkeygen (see
# docker/docker-compose.yml's keygen service) — never a real secret, but
# still never committed.
docker/.dev-secrets/

.idea/
.vscode/
*.swp
